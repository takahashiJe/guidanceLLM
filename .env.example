# --- PostgreSQL Database Settings ---
# データベース名、ユーザー名、パスワードを任意に設定
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# --- Redis Settings (for Celery & other uses) ---
# 開発時はデフォルトでOK
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=

# --- Ollama Settings ---
# 開発時はデフォルトでOK
OLLAMA_HOST=

# --- OSRM/Nominatim Settings (if needed in code) ---
# アプリケーションコードから直接URLを叩く際の参考値
OSRM_CAR_HOST=
OSRM_FOOT_HOST=
NOMINATIM_HOST=