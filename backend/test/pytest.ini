[pytest]
addopts = -ra
testpaths = .
python_files = test_*.py
markers =
    db: tests that require a PostgreSQL DB (PostGIS)
    osrm: tests that require OSRM services
    integration: cross-module tests hitting external services/DB
    slow: may take longer to run
